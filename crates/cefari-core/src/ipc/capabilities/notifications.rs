capability! {
    name: notifications,
    order: 80,
    commands: [
        Notification(NotificationCommand),
    ],
    results: [
        Notification(NotificationResult),
    ],
    events: [
        Notification(NotificationEvent),
    ],
}
