# Cefari Playground

A stress-oriented Cefari app with feature modules grouped by category. Each module can be run from the web UI and reports status, errors, logs, and output inline.

```sh
deno task dev
```

The UI intentionally uses simple Preact components with minimal spacing and separators.
