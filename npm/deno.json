{
  "imports": {
    "cmd-ts": "npm:cmd-ts@^0.15.0",
    "vite": "npm:vite@^8.0.16"
  },
  "tasks": {
    "build:npm": "deno run -A build_npm.ts",
    "bin:install": "deno task build:npm && deno install --config deno.json -f -g -n cefari --allow-all ./dist/npm/esm/bin/cefari.js"
  }
}
