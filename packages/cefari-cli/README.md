# @cefari/cli

TypeScript implementation of the developer-facing `cefari` command.

The public command surface is intentionally small during Cefari's pre-alpha:

- `cefari init`
- `cefari dev`
- `cefari build`
- `cefari package`

Release management commands live under `cefari package`.
